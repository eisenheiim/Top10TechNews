[project]
name = "connectsummary"
version = "0.1.0"
description = "LangGraph pipeline: HN + RSS AI digest for UI"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "langchain-openai>=1.4.1",
    "langgraph>=1.2.10",
    "pydantic>=2.13.4",
    "python-dotenv>=1.2.2",
]
